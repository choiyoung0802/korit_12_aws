server:
  port: 80
spring:
  datasource:
    url: jdbc:mysql://instagram-db.cp24824qedy2.ap-northeast-2.rds.amazonaws.com:3306/instagram
    username: 
    password: 
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
  cloud:
    aws:
      credentials:
        access-key: ${AWS_ACCESS_KEY}
        secret-key: ${AWS_SECRET_KEY}
      s3:
        bucket: choiyoung0802-instagram-uploaded-files
      region:
        static: ap-northeast-2